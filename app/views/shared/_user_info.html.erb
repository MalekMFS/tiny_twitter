
<%= link_to gravatar_for(current_user, size: 50), current_user %>
<%= current_user.name %>
<span><%= link_to "مشاهده نمایه", current_user %></span>
<span><%= current_user.microposts.count %> پست</span>
