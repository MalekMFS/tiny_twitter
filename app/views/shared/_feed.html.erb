<% if @feed_items.any? %>
<ol class="microposts">
  <%= render @feed_items %> <%# renders microposts/_micropost.html.erb%>
</ol>
<%= will_paginate @feed_items, previous_label: "قبلی", next_label: "بعدی" %>
<% end %>
