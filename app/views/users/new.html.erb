<% provide(:title, 'ثبت نام') %>
<h1>ثبت نام</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "اکانتم را بساز!", class:"btn btn-primary" %>
    <% end %>
    
  </div>
</div>
