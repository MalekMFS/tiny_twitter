<% provide(:title, "کاربران") %>
<h1>کاربران</h1>

<%= will_paginate @users, previous_label: "قبلی", next_label: "بعدی" %>
<ul class="users">
  <!-- <% @users.each do |user| %>
    <!-- rails automatically find partial name by class of user object -->
    <!-- <%= render user %>
  <% end %> -->
  <%= render @users %>
</ul>
<%= will_paginate @users, previous_label: "قبلی", next_label: "بعدی" %>
